[project]
name = "welstra"
version = "0.1.0"
description = "AI-powered job matching platform with agent pipeline"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.104.1",
    "uvicorn>=0.35.0",
    "pydantic==2.5.0",
    "pypdf==6.9.2",  # For PDF extraction
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
]

[tool.uv]
package = false